# 医疗知识图谱分析平台

本项目是一个基于Python的医疗知识图谱分析平台。它利用 `NetworkX`、`Pandas` 和 `Matplotlib` 等库，对医疗领域的实体和关系数据进行深入的图分析。平台实现了多种经典的图算法，包括 **PageRank**、**HITS** 和 **Louvain社区发现**，旨在从复杂的知识网络中提取有价值的业务洞察。

## ✨ 主要功能

- **基础图分析**: 计算节点的度、入度、出度，并分析其分布特征，识别网络的宏观结构（如无标度网络特性）。
- **PageRank 分析**: 评估网络中每个节点的“全局重要性”，识别出整个知识体系中的核心概念。
- **HITS 分析**: 区分节点的“权威性”（Authority）和“枢纽性”（Hub），识别出高质量信息的提供者和优质信息的聚合者。
- **Louvain 社区发现**: 对图进行社区划分，识别出功能上或语义上高度相关的节点集群，并分析其内部结构和业务含义。
- **自动化报告生成**: 每个分析模块都能自动生成详细的 Markdown 分析报告和配套的可视化图表，便于理解和分享。

## 📄 分析报告

您可以直接点击以下链接查看各个模块生成的最新分析报告：

- [**基础图分析报告**](./reports/graph_analysis.md)
- [**PageRank 分析报告**](./reports/pagerank_analysis.md)
- [**HITS 分析报告**](./reports/hits_analysis.md)
- [**Louvain 社区发现报告**](./reports/louvain_analysis.md)

## 🚀 如何开始

### 1. 环境准备

本项目推荐使用 `virtualenv` 或 `conda` 创建独立的 Python 环境。

```bash
# 创建一个新的虚拟环境（推荐）
python -m venv .venv

# 激活虚拟环境
# macOS / Linux
source .venv/bin/activate
# Windows
# .\.venv\Scripts\activate
```

### 2. 安装依赖

所有必需的 Python 库都记录在 `requirements.txt` 文件中。

```bash
# 确保你的虚拟环境已激活
pip install -r requirements.txt
```

## 🛠️ 如何使用

所有分析脚本都位于 `src/` 目录下，并应作为模块从项目根目录运行。

### 1. 运行基础图分析

```bash
python -m src.graph_analysis
```

### 2. 运行 PageRank 分析

```bash
python -m src.pagerank_analysis
```

### 3. 运行 HITS 分析

```bash
python -m src.hits_analysis
```

### 4. 运行 Louvain 社区发现

```bash
python -m src.louvain_analysis
```

## 📊 输出结果

每次成功运行分析脚本后，相关的输出会自动保存到 `output/` 和 `reports/` 目录中。

- **分析报告**:
  - 详细的 Markdown 分析报告位于 `reports/` 目录下。
  - 例如: `reports/pagerank_analysis.md`

- **可视化图表**:
  - 所有生成的图表（PNG格式）位于 `output/images/` 下的对应子目录中。
  - 例如: `output/images/pagerank/`

- **中间数据**:
  - 计算过程中产生的得分、分配等数据（CSV/JSON格式）位于 `output/data/` 下的对应子目录中。
  - 例如: `output/data/pagerank/`

## 📂 项目结构

```
.
├── data/                  # 原始数据 (节点和边)
│   ├── edges.csv
│   └── nodes.csv
├── output/                # 所有生成文件的根目录
│   ├── data/              # 中间数据 (CSV, JSON)
│   └── images/            # 可视化图表 (PNG)
├── reports/               # 最终的 Markdown 分析报告
├── src/                   # 源代码
│   ├── data_loader.py     # 数据加载和路径管理
│   ├── graph_analysis.py  # 基础图分析
│   ├── hits_analysis.py   # HITS 算法分析
│   ├── louvain_analysis.py# Louvain 社区发现
│   ├── pagerank_analysis.py # PageRank 算法分析
│   └── set_chinese_plot.py# Matplotlib 中文显示配置
├── .venv/                 # Python 虚拟环境 (推荐)
├── LICENSE                # 项目许可证
├── README.md              # 项目说明文件
└── requirements.txt       # Python 依赖列表
```
